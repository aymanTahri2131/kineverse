VITE_API_URL=http://localhost:5000/api
VITE_APP_NAME=KinéVerse

# Cloudinary Configuration (Optional - only needed for private models or uploads)
# Get these from: https://console.cloudinary.com/
# For public read-only models, only CLOUD_NAME is needed in the code
VITE_CLOUDINARY_CLOUD_NAME=doq0mdnkz
# VITE_CLOUDINARY_API_KEY=your-api-key (Keep this server-side only!)
# VITE_CLOUDINARY_API_SECRET=your-api-secret (Never expose in frontend!)
